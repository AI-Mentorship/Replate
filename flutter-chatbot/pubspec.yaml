name: flutter_chatbot
description: A Flutter cooking chatbot app
publish_to: 'none' # This is a private app

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.3.0
  flutter_tts: ^3.8.5
  http: ^1.2.2
  image_picker: ^1.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
